harnessApiVersion: '1.0'
type: APPLICATION_DEFAULTS
defaults:
- name: STAGING_PATH
  value: $HOME/${app.name}/${service.name}/${serviceTemplate.name}/staging/${timestampId}
- name: BACKUP_PATH
  value: $HOME/${app.name}/${service.name}/${serviceTemplate.name}/backup/${timestampId}
- name: RUNTIME_PATH
  value: $HOME/${app.name}/${service.name}/${serviceTemplate.name}/runtime
